# Benchmarks

This directory contains scripts for measuring experiment-system overhead and benchmark procedures.

## Current benchmark
- `benchmark_runner_overhead.py`: measures repeated no-op experiment execution latency.

## Run
```bash
uv run python benchmarks/benchmark_runner_overhead.py
```
